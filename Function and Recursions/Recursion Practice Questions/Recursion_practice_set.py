# Easy

# Print numbers from n to 1.
# Print numbers from 1 to n.
# Find factorial of n.
# Find sum of first n natural numbers.
# Find a^b (power).
# Count digits in a number.
# Sum digits of a number.
# Reverse a string.
# Check if a string is palindrome.
# Find nth Fibonacci number (basic recursion).


# Medium

# Find GCD of two numbers using recursion.
# Perform recursive binary search in a sorted list.
# Find product of two numbers using only recursion (a*b as repeated addition).
# Print all elements of a list recursively.
# Find maximum element in a list recursively.
# Check if array/list is sorted (ascending).
# Remove all occurrences of a character from a string recursively.
# Replace "pi" with "3.14" in a string recursively.
# Count occurrences of a target in a list recursively.
# Generate all subsequences of a string.


# Hard

# Tower of Hanoi (print all moves).
# Generate all permutations of a string.
# Solve N-Queens (backtracking recursion).
# Solve Sudoku (recursive backtracking).
# Rat in a Maze (all valid paths).
# Word search in a 2D board (DFS recursion).
# Generate all balanced parentheses for n pairs.
# Recursive merge sort.
# Recursive quicksort.
# Find kth symbol in grammar / similar recursive pattern problem.